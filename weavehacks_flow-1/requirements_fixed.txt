# Core dependencies with pinned versions to avoid dependency resolution issues
# Python 3.9 compatible versions

# CrewAI and its dependencies - using older stable version
crewai==0.1.40
langchain==0.1.1
langchain-community==0.0.20
langsmith==0.0.83

# Pydantic - pin to v1 for compatibility
pydantic>=1.10.0,<2.0.0

# Scientific computing
numpy==1.24.4
matplotlib==3.7.5
scipy==1.10.1

# HTTP and API
requests==2.31.0

# Weights & Biases
wandb==0.16.6
weave==0.50.0

# Audio processing
sounddevice==0.4.6
soundfile==0.12.1

# Voice recognition - lighter alternative
# openai-whisper is heavy, using whisper directly
whisper==1.1.10

# UI
streamlit==1.32.2

# Testing
pytest==8.0.2

# Computer vision
opencv-python==4.8.1.78

# Additional commonly needed dependencies
pandas==1.5.3
plotly==5.20.0
python-dotenv==1.0.1

# FastAPI dependencies for backend
fastapi==0.111.0
uvicorn==0.29.0
pydantic-settings==2.2.1