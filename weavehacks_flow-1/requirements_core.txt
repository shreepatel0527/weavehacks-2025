# Core requirements only - no CrewAI, no Whisper, minimal dependencies
# This should install quickly without dependency resolution issues

# Core Python utilities
pydantic>=1.10.0,<2.0.0
python-dotenv==1.0.1
requests==2.31.0

# Scientific computing (essential for chemistry calculations)
numpy==1.24.4
scipy==1.10.1

# Data and visualization (for UI)
pandas==1.5.3
matplotlib==3.7.5
plotly==5.20.0

# Web UI
streamlit==1.32.2

# Audio (lightweight)
sounddevice==0.4.6
soundfile==0.12.1

# Testing
pytest==8.0.2

# Backend API
fastapi==0.111.0
uvicorn==0.29.0

# Note: Skipping these problematic packages:
# - crewai (complex langchain dependencies)
# - openai-whisper (huge download, many dependencies)
# - opencv-python (can be installed separately if needed)
# - wandb/weave (optional logging, can work without)