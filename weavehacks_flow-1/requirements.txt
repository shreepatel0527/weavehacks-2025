# WeaveHacks Flow Requirements
# Updated to avoid dependency resolution issues

# Core dependencies with version constraints
pydantic>=1.10.0,<2.0.0
numpy>=1.24.0,<1.25.0
matplotlib>=3.7.0,<3.8.0
scipy>=1.10.0,<1.11.0
requests>=2.31.0,<3.0.0

# Optional but recommended
pandas>=1.5.0,<2.0.0
python-dotenv>=1.0.0

# Audio support (comment out if not needed)
sounddevice>=0.4.6
soundfile>=0.12.1

# UI support (comment out if not using streamlit)
streamlit>=1.32.0,<2.0.0
plotly>=5.20.0,<6.0.0

# Testing
pytest>=8.0.0,<9.0.0

# Computer vision (comment out if not needed)
opencv-python>=4.8.0,<5.0.0

# API backend (comment out if not needed)
fastapi>=0.111.0,<1.0.0
uvicorn>=0.29.0,<1.0.0

# Note: The following packages are NOT included due to dependency conflicts:
# - crewai: Has complex langchain dependencies that cause resolution issues
# - openai-whisper: Very large package with many dependencies
# - wandb/weave: Optional logging, the code works without them
#
# To install these separately if needed:
# pip install --user crewai==0.1.40
# pip install --user openai-whisper
# pip install --user wandb weave